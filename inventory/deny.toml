# cargo-deny configuration for the inventory workspace.
#
# Referenz: https://embarkstudios.github.io/cargo-deny/checks/cfg.html
# Aktiviert vom CI-Workflow .github/workflows/security.yml (cargo-deny job).
#
# Sicherheits-Quellen:
#   - NIST SP 800-204C §3.2 + §3.4 (CI integrity, dependency pinning)
#   - OWASP Top-10 A06:2021 (Vulnerable and Outdated Components)
#   - SLSA Build L3 (pinned dependencies, vetted licenses)

[graph]
# Wir prüfen alle Target-Tripel, damit OS-spezifische Crates nicht
# unentdeckt durchrutschen.
all-features = false
no-default-features = false

[advisories]
# cargo-deny v0.19+ Schema: severity-String-Felder
# (vulnerability/unmaintained/notice) sind entfallen.
# Vulnerabilities werden per Default als Fehler behandelt.
# Schema-Migration: `version = 2` Pflicht.
# Quelle: cargo-deny 0.14+-Release-Notes, enforced in 0.19.x.
# Doku: https://embarkstudios.github.io/cargo-deny/checks/advisories/cfg.html
version = 2
db-path = "~/.cargo/advisory-db"
db-urls = ["https://github.com/RustSec/advisory-db"]
# Lock-File referenziert yanked-Version → Build bricht.
yanked = "deny"
ignore = [
    # False positives: these advisories address the published
    # `inventory` crate on crates.io (plugin-pattern library).
    # Our workspace-root crate happens to share the name but is
    # path-sourced, not a crates.io dependency. cargo-audit
    # matches on (name, version) without source-discrimination.
    # Tracked for a future crate-rename ADR (e.g. home-inventory).
    "RUSTSEC-2023-0057",
    "RUSTSEC-2023-0058",
]

[licenses]
# cargo-deny v0.19+ Schema: severity-String-Felder
# (unlicensed/default/copyleft) sind entfallen.
# Stattdessen wirkt eine reine Allow-Liste; alles nicht
# Gelistete bricht den Build.
# Schema-Migration: `version = 2` Pflicht.
# Hauptprojekt steht unter MIT — GPL/AGPL bewusst nicht in
# der Allow-Liste, damit Copyleft-Code nicht einziehen kann.
version = 2
confidence-threshold = 0.93
allow = [
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "CC0-1.0",
    "ISC",
    "MIT",
    "Unicode-3.0",
    "Unicode-DFS-2016",
    "Zlib",
]
# Crates ohne SPDX-Tag (z.B. unicode-ident, ring) bei Bedarf
# explizit einzelfreigeben — hier vorerst leer.
exceptions = []

[bans]
# Single-Version-Enforcement: Cargo-Deny meldet, wenn mehrere
# Versionen desselben Crate eingezogen sind (Crypto-Stack-Drift,
# CVE-Triage-Lücken). "warn" statt "deny" um Workspace-Realität
# nicht zu blockieren — Operator entscheidet Fall-zu-Fall.
multiple-versions = "warn"
wildcards = "deny"
highlight = "all"
# Komplett-Bans bewusst leer: ring/openssl-sys sind transitiv
# über rustls/native-tls eingezogen und nicht realistisch
# bannbar. Single-Version-Enforcement greift via
# multiple-versions oben.
deny = []
skip = []
skip-tree = []

[sources]
# Keine Git-Repo-Direktreferenzen, keine alternativen Registries.
# Begründung: Supply-Chain-Integrität — Crate-Source muss
# crates.io sein, sonst ist die Provenance unkontrolliert.
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
