# Shell-Scripts immer mit LF auschecken — sie laufen auf Linux-Hosts
# (Raspberry Pi, inventory-Build-Server, VPS). CRLF wuerde die Shebang
# brechen ("/usr/bin/env bash\r": bad interpreter).
*.sh text eol=lf
