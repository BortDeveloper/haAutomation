# Niemals committen
**/age.key
**/*.age
**/vpn.tailscale.env
**/vpn.netbird.env
**/wg0.conf
**/authgate.env

# Edge-Klasse-Klartext (Closing-Brief §2.1) — diese Pfade dürfen nur
# sops-verschlüsselt (.enc) im Repo liegen, niemals im Klartext.
**/coordinator_backup.json
**/zigbee2mqtt/*.yaml
**/*secrets.yaml
**/flows_cred.json
**/mosquitto/passwd
**/mosquitto/acl
**/*tokens.json

!**/*.example
!**/*.enc

# Build-Output
target/
*.tmp
.DS_Store

# Daten (laufzeit)
**/data/
**/inventory.db
**/inventory.db-journal
**/inventory.db-wal

# Lokales Test-/Dev-Verzeichnis (Doku: docs/getting-started.md, Phase 2-3):
# enthaelt persoenliche URLs, Tokens, lokale DB. Niemals committen —
# `.env.example` und `smoke-test.sh` liegen daneben unter inventory/.
**/local/

# IDE
.vscode/
.idea/

# Claude Code lokaler State (memory-Snapshots werden explizit eingecheckt)
.claude/*
!.claude/memory/
.claude/worktrees/
